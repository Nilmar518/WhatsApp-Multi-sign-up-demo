# Production overrides — applied during `vite build` for Firebase Hosting deployment.
# These override .env values. Non-overridden vars are inherited from .env.

# Instagram OAuth redirect must point to the Railway backend, not ngrok
VITE_IG_REDIRECT_URI=https://backend-production-9fd0.up.railway.app/integrations/instagram/oauth-callback
