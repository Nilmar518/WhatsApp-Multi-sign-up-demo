{
  "name": "migo-uit",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm -r run build",
    "lint:backend": "pnpm --filter @migo-uit/backend run lint",
    "lint:frontend": "pnpm --filter @migo-uit/frontend run lint",
    "deploy:indexes": "npx firebase-tools deploy --only firestore:indexes --project smart-service-85369"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "esbuild",
      "protobufjs"
    ]
  },
  "dependencies": {
    "@nestjs/bull": "^11.0.4",
    "@nestjs/event-emitter": "^3.0.1",
    "@nestjs/schedule": "^6.1.1",
    "bull": "^4.16.5",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "@types/bull": "^4.10.4"
  }
}
